{
  "name": "configure-credentials",
  "version": "0.0.1",
  "description": "Github action for configuring emulator.wtf credentials using a GitHub OIDC token",
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "homepage": "https://github.com/emulator-wtf/configure-credentials",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.4.5",
    "rimraf": "^6.0.1"
  }
}
